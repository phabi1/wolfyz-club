import { DetailItem } from './detail-item';

describe('DetailsItem', () => {
  it('should create an instance', () => {
    const directive = new DetailItem();
    expect(directive).toBeTruthy();
  });
});
