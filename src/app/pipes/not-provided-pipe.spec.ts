import { NotProvidedPipe } from './not-provided-pipe';

describe('NotProvidedPipe', () => {
  it('create an instance', () => {
    const pipe = new NotProvidedPipe();
    expect(pipe).toBeTruthy();
  });
});
